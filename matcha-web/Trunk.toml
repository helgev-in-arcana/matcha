[build]
target = "index.html"
dist = "dist"

[serve]
port = 8080
open = false

# Deploying under a subpath (GitHub Pages project sites) needs the asset URLs
# rewritten:
#   trunk build --release --public-url /<repo-name>/
